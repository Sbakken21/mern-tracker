import React from 'react';

const About = () => {
    return (
        <div className="container">
            <h1>About</h1>
            <p>FTrak is designed using the MERN stack (MongoDB for a database, Express as a backend framework, React for the frontend, and NodeJS).</p>
        </div>
    );
};

export default About;